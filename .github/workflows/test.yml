name: EVA Cup Reminder

on:
  schedule:
    - cron: '0 5 * * *'

  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - run: pip install -r requirements.txt

      - run: python cuptracker.py
